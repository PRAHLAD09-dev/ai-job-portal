import { Card } from "@/components/ui/card";

export default function RecruiterCompanyPage() {
  return (
    <Card>
      <h1 className="text-lg font-semibold">RecruiterCompanyPage</h1>
      <p className="mt-1 text-sm text-[hsl(var(--muted))]">
        Scheduled for a later phase per 05_FRONTEND_ROADMAP.md.
      </p>
    </Card>
  );
}
